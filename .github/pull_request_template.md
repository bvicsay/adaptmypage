## What

## Why

## Checklist
- [ ] `pnpm typecheck && pnpm test && pnpm build` pass
- [ ] SDK change has a test
- [ ] Nothing new is collected from the browser beyond semantic events
- [ ] If `buildModelState` or `DEFAULT_QUESTIONS` changed: before/after example below
